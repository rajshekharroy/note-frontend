// import axios from "axios";

// const BASE_URL = "https://note-backend-three.vercel.app/api";

// export const fetchAllNotes = (page = 1, query = "") =>
//   axios.get(`${BASE_URL}/get?page=${page}&limit=6&query=${query}`);

// export const addNote = (note) => axios.post(`${BASE_URL}/add`, note);

// export const updateNote = (id, updatedNote) =>
//   axios.patch(`${BASE_URL}/edit/${id}`, updatedNote);

// export const updatePin = (id, isPinned) =>
//   axios.patch(`${BASE_URL}/update-pin/${id}`, isPinned);

// export const deleteNote = (id) => axios.delete(`${BASE_URL}/delete/${id}`);
